import React from 'react';
import { Box, Typography } from '@mui/material';

const GraficosDemo: React.FC = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h6">Funcionalidade de gráficos removida</Typography>
      <Typography variant="body2" color="text.secondary">Os recursos de criação e edição de gráficos foram descontinuados conforme solicitado.</Typography>
    </Box>
  );
};

export default GraficosDemo;
