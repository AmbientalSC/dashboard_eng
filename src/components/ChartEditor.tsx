import React from 'react';
import { Box, Typography } from '@mui/material';

const ChartEditor: React.FC = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h6">Editor de gráficos removido</Typography>
      <Typography variant="body2" color="text.secondary">A funcionalidade de criação/edição de gráficos foi removida conforme solicitado.</Typography>
    </Box>
  );
};

export default ChartEditor;

