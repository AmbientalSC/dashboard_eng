import React from 'react';
import { Box, Typography } from '@mui/material';

const ChartViewer: React.FC = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="body2" color="text.secondary">Componente de visualização de gráficos removido.</Typography>
    </Box>
  );
};

export default ChartViewer;
